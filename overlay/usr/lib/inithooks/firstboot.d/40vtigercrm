#!/bin/bash -e
# set vtigercrm admin password and domain to serve

. /etc/default/inithooks

[ -e $INITHOOKS_CONF ] && . $INITHOOKS_CONF
$INITHOOKS_PATH/bin/vtigercrm.py --pass="$APP_PASS" --domain="$APP_DOMAIN"

